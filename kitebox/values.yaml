# Default values for kitebox.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
replicaCount: 1
image:
  repository: heliostech/kite
  tag: latest
  pullPolicy: IfNotPresent
